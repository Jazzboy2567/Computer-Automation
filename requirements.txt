# Core runtime (no API keys required — StubProvider + smoke tests work with just these).
playwright>=1.44
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
jinja2>=3.1
python-multipart>=0.0.9
httpx>=0.27

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23

# Optional real providers (uncomment to enable):
# anthropic>=0.40
# openai>=1.30
